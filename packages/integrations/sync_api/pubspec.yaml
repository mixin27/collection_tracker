name: sync_api
description: "Sync API integration for backend synchronization."
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  dio: ^5.9.0
  flutter:
    sdk: flutter
  storage:
    path: ../storage

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
